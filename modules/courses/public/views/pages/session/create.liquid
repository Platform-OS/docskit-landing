---
layout: modules/courses/courses
slug: session/create
method: post
---
{% liquid
  function res = 'modules/user/lib/commands/session/create', email: context.params.email, password: context.params.password
  if res.valid
    redirect_to '/course'
  else
    theme_render_rc 'components/organisms/user-login-form', errors: res.errors
  endif
%}
