{% liquid
  function object = 'modules/courses/commands/lesson/delete/check', object: object

  if object.valid
    graphql object = 'modules/courses/videos/delete', args: object
    hash_assign object['valid'] = true
  endif

  return object
%}
