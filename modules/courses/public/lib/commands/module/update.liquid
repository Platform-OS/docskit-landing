{% liquid
  function object = 'modules/courses/commands/module/update/build', object: object
  function object = 'modules/courses/commands/module/update/check', object: object

  if object.valid
    graphql object = 'modules/courses/module/update', args: object
    hash_assign object['valid'] = true
  endif

  return object
%}
